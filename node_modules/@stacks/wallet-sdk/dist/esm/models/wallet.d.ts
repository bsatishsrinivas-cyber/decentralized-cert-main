import { NetworkClientParam } from '@stacks/network';
import { Wallet } from './common';
export interface LockedWallet {
    encryptedSecretKey: string;
}
export declare function restoreWalletAccounts({ wallet, gaiaHubUrl, network, client: _client, }: {
    wallet: Wallet;
    gaiaHubUrl: string;
} & NetworkClientParam): Promise<Wallet>;
